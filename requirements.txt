# AutoSort Python dependencies.
# Install into a Python 3.12 venv:  pip install -r requirements.txt
# (or just run ./scripts/install.sh)

# LeRobot with Feetech motor support — SO-ARM101 uses Feetech servos.
# Pulls in the full pipeline: lerobot-find-port / calibrate / teleoperate /
# record / train / rollout / edit-dataset, plus ACT policy + OpenCV cameras.
lerobot[feetech]

# Hugging Face Hub CLI — `hf auth login`, dataset/model push & pull.
huggingface_hub[cli]
